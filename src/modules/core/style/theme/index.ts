import {Colors} from "../variables/colors";

export const theme = {
    colors: {
        primary: '',
        secondary: ''
    },
    background: {
        black: Colors.BLACK
    },
    messages: {
        default: Colors.DARK_BLUE,
        main: Colors.DARK_GREEN,
    }
}
