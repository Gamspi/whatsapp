import styled from "styled-components";

const Container = styled.div`
  width: 40rem;
  max-height: 100%;
  overflow-x: hidden;
  overflow-y: auto;
`
export const StyledContactsLayout = {
    Container
}
